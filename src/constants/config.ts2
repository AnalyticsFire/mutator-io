import { TopicTransformers } from './'
import PassThroughTransformer from '../transformers/pass-through'

const config : TopicTransformers = {
  'mytopic': {
    transformer: new PassThroughTransformer()
  }
}

/*

TODO: creare config yml con solo topic -> nometransformer e creare funzione per validare il tipo dei transformer
vedere se si puo fare con typescript

*/

export default config
